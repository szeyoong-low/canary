name: Deploy to development
# Adapted from https://developer.hashicorp.com/terraform/tutorials/automation/github-actions

on:
  pull_request:
    branches:
      - main

permissions: {}

env:
  TF_API_TOKEN: "${{ secrets.TF_API_TOKEN }}"
  TF_CLOUD_ORGANIZATION: "${{ vars.TF_CLOUD_ORGANIZATION }}"
  BOT_NAME: Bot
  PR_NUMBER: "${{ github.event.pull_request.number }}"
  TF_WORKSPACE_DEVELOPMENT_PR: "${{ vars.TF_WORKSPACE_DEV_PREFIX }}${{ github.event.pull_request.number }}"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}

jobs:
  reconcile-ephemeral-workspaces:
    permissions:
      contents: read
      pull-requests: write

    uses: ./.github/workflows/reconcile-workspaces.yml
    secrets:
      TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}
    with:
      pull_request_number: ${{ github.event.pull_request.number }}

  # Builds and pushes the image, then reports the tag it used. Called rather than
  # left to trigger itself, so that the image is in ECR before the task definition
  # below names it.
  backend:
    permissions:
      contents: read

      # A reusable workflow can never hold more than the job that calls it grants.
      id-token: write

    uses: ./.github/workflows/backend.yml
    secrets:
      ECR_REPOSITORY_NAME: ${{ secrets.ECR_REPOSITORY_NAME }}
      AWS_ECR_ROLE_ARN: ${{ secrets.AWS_ECR_ROLE_ARN }}

  # Called rather than left to trigger itself, so that the production plan is
  # only spent on a pull request whose image actually builds. It pins the
  # release tag itself, so it never reads the build's output.
  plan-production:
    permissions:
      contents: read
      pull-requests: write

    needs: backend

    uses: ./.github/workflows/plan-production.yml
    secrets:
      TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}

  deploy-development-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    needs:
      - reconcile-ephemeral-workspaces
      - backend

    # The build is skipped for pull requests from forks, which hold no secrets
    # and so cannot assume the push role. Its output is then empty, which would
    # otherwise pin the task definition to a tag that does not exist.
    if: needs.backend.outputs.image-tag != ''

    steps:
      - uses: actions/checkout@v7

      - name: Pin image for task definition
        uses: ./.github/actions/pin-image-tag
        with:
          image-tag: "${{ needs.backend.outputs.image-tag }}"
          directory: terraform/infrastructure

      - name: Upload configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.3.2
        id: apply-upload
        with:
          workspace: ${{ env.TF_WORKSPACE_DEVELOPMENT_PR }}
          directory: terraform/infrastructure

      - name: Create apply run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.3.2
        id: apply-run
        with:
          workspace: ${{ env.TF_WORKSPACE_DEVELOPMENT_PR }}
          configuration_version: ${{ steps.apply-upload.outputs.configuration_version_id }}

      - name: Apply
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.3.2
        if: fromJSON(steps.apply-run.outputs.payload).data.attributes.actions.IsConfirmable
        id: apply
        with:
          run: ${{ steps.apply-run.outputs.run_id }}

      - name: Update PR
        uses: actions/github-script@v9
        id: plan-comment
        env:
          PR_NUMBER: ${{ env.PR_NUMBER }}
          CONCLUSION: ${{ steps.apply.conclusion }}
          RUN_LINK: ${{ steps.apply-run.outputs.run_link }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = [
              `#### HCP Terraform Plan application to Development: ${process.env.CONCLUSION}`,
              `[HCP Terraform Apply](${process.env.RUN_LINK})`,
            ].join("\n");

            await github.rest.issues.createComment({
              issue_number: Number(process.env.PR_NUMBER),
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

  backend-health-check:
    runs-on: ubuntu-latest
    needs: deploy-development-pr

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - uses: ./.github/actions/health-check
        with:
          # Mirrors `backend_hostname` in terraform/infrastructure/dns.tf, which
          # prefixes the pull request number onto the production name to give
          # each ephemeral environment its own subdomain.
          url: "https://dev-${{ env.PR_NUMBER }}-${{ vars.BACKEND_API_HOST }}/health"
